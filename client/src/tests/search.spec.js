describe('autocomplete', () => {
  it('list tags from backend', async () => {})

  it('only show max 5 suggestions', async () => {})

  it('select with enter', async () => {})

  it('dont suggest already selected tag', async () => {})

  it('dont accept already selected tag on enter', async () => {})

  it('tab ads first suggestion by default', async () => {})

  it('arrow up suggestion up', async () => {})

  it('arrow down suggestion down', async () => {})

  it('mouse hover suggestion to mouse position', async () => {})

  it('tab ads current highlighted position', async () => {})

  it('display selected tags under search bar', async () => {})

  it('remove selected tag pressing it', async () => {})

  it('removed selected tag shows in suggestions', async () => {})
})

describe('index page start animation', () => {
  it('only perform start animation once', async () => {})
})
