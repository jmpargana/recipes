
describe('upload form validation', () => {
  xit('parse time: render error', async () => {

  })

  // it('parse time: allow numerical values', async () => {})

  // it('add ingridient: must provide name and amount', async () => {})

  // it('add ingridient: append to list', async () => {})

  // it('add ingridient: no copy of last object', async () => {})

  // it('tags: append to recipe', async () => {})

  // it('tags: no duplicates', async () => {})

  // it('submit: missing title error', async () => {})

  // it('submit: at least one tag', async () => {})

  // it('submit: validate method', async () => {})

  // it('submit: validate time', async () => {})

  // it('submit: at least three ingridients', async () => {})

})
